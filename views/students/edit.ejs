<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Student</title>
</head>
<body>
    <h1>Edit Student</h1>
    <form action="/students/<%= student._id %>?_method=PUT" method="POST">
        <div>
            <label for="name">Name : </label>
            <input type="text" id="name" name="student[name]" value="<%= student.name %>">
        </div>
        <div>
            <label for="class">class : </label>
            <input type="text" id="class" name="student[class]" value="<%= student.class %>">
        </div>
        <div>
            <label for="roll">Roll : </label>
            <input type="text" id="roll" name="student[roll]" value="<%= student.roll %>">
        </div>
        <div>
            <label for="section">section : </label>
            <input type="text" id="section" name="student[section]" value="<%= student.section %>">
        </div>
        <button>Update Student</button>
    </form>
    <a href="/students/<%= student._id %> ">Back to Students</a>
</body>
</html>